/*
문자를 숫자로 바꾸기
Number(값) / parseInt(값)
*/

function solution(s) {
  return Number(s);
}

let result = solution('-1234');
console.log(result);

function strToInt(str) {
  return str / 1;
}

function strToInt(str) {
  return +str;
}
